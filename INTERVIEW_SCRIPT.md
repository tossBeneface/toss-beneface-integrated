# INTERVIEW_SCRIPT.md — 실전 발언 스크립트

> **사용법:** 각 질문은 "한 문장 핵심 → 기술 근거 → 결과" 순서로 말한다.
> 리스크 발언(MZ, 열정 식음, 세대론)은 아래 교정 버전으로만 답한다.

---

## 1. 자기소개 / 지원 동기

**스크립트:**
> "저는 백엔드와 AI 파이프라인을 함께 설계할 수 있는 엔지니어입니다.
> 이번 프로젝트에서 Spring Boot와 FastAPI를 Kafka로 연결하고,
> 핵심 계산 로직을 Rust로 내려서 배치 처리 속도를 Python 대비 약 16배 끌어올렸습니다.
> 기능 구현에서 멈추지 않고 성능 측정과 운영 가시성까지 챙기는 방식으로 일합니다."

---

## 2. 열정 / 방향 전환 (리스크 질문)

**위험 버전 (절대 사용 금지):**
~~"예전엔 블록체인에 열정이 있었는데 식었어요..."~~

**교정 스크립트:**
> "블록체인을 공부하면서 '기술 자체'보다 '기술이 사람의 문제를 어떻게 푸는가'에
> 더 관심이 생겼고, 그 교차점이 AI 백엔드였습니다.
> 지금은 추천·결제·음성이라는 실제 유저 접점에서 AI가 작동하는 시스템을 만들고 있습니다."

---

## 3. 세대·협업 스타일 (리스크 질문)

**위험 버전 (절대 사용 금지):**
~~"저는 MZ 세대라서 수평적 문화를 선호합니다..."~~

**교정 스크립트:**
> "협업에서 제가 중요하게 여기는 건 '블로커를 빠르게 제거하는 것'입니다.
> 코드 리뷰든, 기술 선택이든, 팀 전체가 막히는 지점을 먼저 치우는 사람이 되려고 합니다.
> 수직이든 수평이든 구조보다는 팀의 속도와 결과에 집중하는 편입니다."

---

## 4. Rust + PyO3 통합 (기술 심화 질문)

**스크립트:**
> "Rust 엔진의 진짜 강점은 단건 호출이 아니라 배치 처리입니다.
> 단건은 PyO3 JSON 경계 비용이 계산 비용을 초과하지만,
> 100개 가맹점 배치에서는 Python 순차 루프 대비 약 16배 빠릅니다 (41ms → 2.6ms).
> 이 차이는 결제 시 여러 가맹점의 최적 카드를 실시간으로 추천할 때 p99 레이턴시에 직접 영향을 줍니다."

**꼬리 질문: "왜 단건은 느리냐"**
> "Python → Rust 호출 시 구조체를 JSON으로 직렬화하고, Rust 내부에서 역직렬화합니다.
> 후보 5개 기준으로 이 오버헤드가 계산 시간보다 큽니다.
> 이 문제는 단건이 아니라 배치로 묶어서 한 번만 경계를 넘기는 방식으로 해결했습니다."

---

## 5. gRPC (Spring ↔ FastAPI 통신)

**스크립트:**
> "카드 혜택 추천처럼 결제 직전에 호출되는 경로는 레이턴시가 매우 민감합니다.
> HTTP/1.1 JSON 대신 gRPC Protobuf를 쓰면 페이로드 크기가 줄고 HTTP/2 멀티플렉싱으로
> 커넥션 오버헤드가 사라집니다.
> Spring Boot는 grpc-spring-boot-starter로 stub을 자동 생성하고,
> FastAPI 쪽은 grpc.aio 서버로 비동기로 처리합니다."

---

## 6. Kafka + Outbox 패턴

**스크립트:**
> "결제 완료 후 주문, 재고, 정산이 각자 독립적으로 처리돼야 하는 구조에서
> Outbox 패턴으로 DB 저장과 이벤트 발행을 같은 트랜잭션으로 묶었습니다.
> 이렇게 하면 메시지 유실 없이 Exactly-Once 성격을 확보할 수 있고,
> 각 Consumer는 장애가 나도 나머지에 영향을 주지 않습니다."

**꼬리 질문: "Exactly-Once를 완전히 보장하냐"**
> "DB 트랜잭션 내에서 아웃박스에 기록하고, 별도 스케줄러가 발행하는 구조라
> 최소 1회 전달은 보장됩니다. Consumer 측은 idempotent하게 설계해서
> 중복 처리가 와도 결과가 바뀌지 않도록 했습니다."

---

## 7. 모니터링 / 장애 대응

**스크립트:**
> "Prometheus + Grafana로 Rust 엔진·Python 폴백 호출 비율,
> p99 레이턴시, Kafka Consumer Lag을 실시간으로 봅니다.
> Rust 엔진이 실패하면 Python 폴백으로 자동 전환되고,
> 이 전환 횟수가 대시보드에 찍히면 배포 후 회귀 탐지 신호로 씁니다."

---

## 8. Spring Batch

**스크립트:**
> "일별 정산처럼 대량 후처리는 온라인 요청 경로와 분리해야 합니다.
> Chunk size 100으로 메모리를 제어하고, JobParameters에 날짜를 넣어
> 같은 날짜 배치의 중복 실행을 DB 레벨에서 막았습니다."

---

## 9. 역질문 (면접 마지막)

우선순위 순서대로 하나만 고른다:

1. **"현재 Python 백엔드를 Java로 전환할 때 가장 큰 페인 포인트는 성능인가요, 유지보수성인가요?"**
   - 회사의 기술 방향과 내 강점(gRPC, Rust 브리지)을 연결하는 질문.

2. **"AI 추론 서버와 비즈니스 로직 서버 간 인터페이스로 현재 어떤 방식을 사용 중이신가요?"**
   - 내가 이미 gRPC + Kafka 두 패턴 모두 경험했음을 자연스럽게 부각.

---

## 리스크 발언 체크리스트 (말하기 전 확인)

- [ ] "열정이 식었다" → 사용 금지
- [ ] "MZ 세대" / "수평적 문화" → 사용 금지  
- [ ] "~하려고 했는데 못 했어요" → 구현 완료된 것만 언급
- [ ] 숫자 없이 "빠르다", "안정적이다" → 반드시 수치 병기 (16x, 41ms→2.6ms 등)
