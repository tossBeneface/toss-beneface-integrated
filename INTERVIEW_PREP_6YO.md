# 🚀 스콘에이아이 2차 면접 필승 가이드 (비유 + 리팩토링 통합본)

김진하님, 이 문서는 **[6세 눈높이 비유]**로 원리를 이해하고, **[실전 Before & After]**로 면접관을 설득할 수 있도록 구성되었습니다.

---

## 1. JWT (비밀 도장이 찍힌 신분증)

### 🐣 6세 눈높이 비유
*   JWT는 **"3가지 칸이 있는 마법 신분증"**이에요.
    1. **머리(Header):** "이건 마법 신분증이야!"라는 이름표.
    2. **몸통(Payload):** "이름: 김진하" 같은 진짜 정보.
    3. **꼬리(Signature):** 엄마만 아는 **비밀 도장**.
*   나쁜 사람이 정보를 몰래 바꿔도 비밀 도장이 안 맞으면 바로 들통나요!

### 🛠 실전 리팩토링 (Before & After)
*   **Before:** 기존 코드는 `Jwts.builder()` 안에 정보들이 섞여 있어, 어디가 Header이고 어디가 Signature인지 한눈에 보이지 않았습니다. 1차 면접 때 이 구조를 혼동했던 원인이기도 했습니다.
*   **After:** `TokenManager.java`를 리팩토링하여 각 파트별로 **[1. Header, 2. Payload, 3. Signature]** 주석을 명시적으로 달고 로직을 분리했습니다.
*   **답변:** "1차 면접 후 JWT의 핵심인 구조적 무결성을 다시 공부했습니다. 코드상에서 Header, Payload, Signature의 역할을 명확히 구분하여 가독성을 높이고 인증의 원리를 코드로 증명했습니다."

---

## 2. 동시성 제어와 비관적 락 (장난감 상자 잠금장치)

### 🐣 6세 눈높이 비유
*   친구 두 명이 동시에 하나의 장난감 상자(돈)를 열려고 하면 싸움이 나요.
*   **비관적 락**은 "내가 다 쓸 때까지 아무도 손대지 마!" 하고 아예 **튼튼한 자물쇠**를 채우는 거예요. 내가 끝날 때까지 다른 친구는 밖에서 줄 서서 기다려야 해요.

### 🛠 실전 리팩토링 (Before & After)
*   **Before:** 기존의 `findById` 방식은 여러 명이 동시에 결제 버튼을 누르면, 같은 '예산(Budget)' 값을 읽어 동시에 차감하면서 계산이 틀려버리는 **경쟁 상태(Race Condition)** 위험이 있었습니다.
*   **After:** `MemberRepository`에 `findByIdWithPessimisticLock`을 추가했습니다. DB 레벨에서 해당 로우(Row)를 점유하여, 한 트랜잭션이 끝날 때까지 다른 요청이 접근하지 못하게 막았습니다.
*   **답변:** "데이터 정합성이 생명인 결제 시스템에서 발생할 수 있는 동시성 문제를 해결하기 위해 비관적 락을 도입했습니다. 이를 통해 대규모 요청 속에서도 예산 차감이 정확하게 이루어지도록 설계했습니다."

---

## 3. AI 서비스 백엔드 (말귀 잘 알아듣는 로봇 친구)

### 🐣 6세 눈높이 비유
*   AI는 똑똑하지만 가끔 외계어를 섞어 말하는 로봇 친구예요.
*   로봇한테 **"꼭 JSON이라는 예쁜 상자에 담아서 말해!"**라고 시키고, 로봇이 흘린 외계어를 치우는 **청소기(Parsing)**와, 로봇이 대답을 못 할 때를 대비한 **예비 간식(Fallback)**을 준비했어요.

### 🛠 실전 리팩토링 (Before & After)
*   **Before:** LLM이 응답에 마크다운 태그(```json)를 포함하거나 형식이 어긋나면 서버에서 파싱 오류가 발생하여 서비스가 중단될 수 있었습니다.
*   **After:** OpenAI의 **JSON Mode**를 강제하고, 정규표현식을 사용한 **견고한 데이터 클렌징**, 그리고 파싱 실패 시 기본 응답을 내보내는 **Fallback 로직**을 추가했습니다.
*   **답변:** "AI 응답의 불확실성을 제어하는 것이 AI 백엔드의 핵심입니다. JSON 모드 강제와 예외 처리 로직을 통해 어떤 상황에서도 사용자에게 안정적인 서비스를 제공할 수 있도록 고도화했습니다."

---

## 💡 면접 마무리 한마디 (필살기)

**"저는 1차 면접 직후 제 코드를 다시 돌아보았습니다. 단순히 기능이 돌아가는 것을 넘어, 대규모 트래픽이 발생하는 글로벌 서비스라면 발생할 수 있는 '동시성 문제'와 'AI 응답의 불확실성'을 해결하는 것이 백엔드 개발자의 핵심 역량이라고 생각했기 때문입니다. 그래서 직접 비관적 락을 도입하고 AI 서빙 로직을 견고하게 리팩토링하며 서비스의 완성도를 높였습니다."**
